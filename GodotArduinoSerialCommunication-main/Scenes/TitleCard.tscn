[gd_scene load_steps=7 format=3 uid="uid://cynu3uauvclm6"]

[ext_resource type="Script" path="res://menu.gd" id="1_1lfjr"]
[ext_resource type="Shader" path="res://Shaders/menu.gdshader" id="2_aj3u2"]
[ext_resource type="Texture2D" uid="uid://dk6k08tdmhkda" path="res://Images/image.png" id="3_ctgpw"]

[sub_resource type="CSharpScript" id="CSharpScript_dxtts"]
script/source = "shader_type canvas_item;

void fragment() {
	vec2 iResolution = 1.0 / SCREEN_PIXEL_SIZE;
	vec2 uv = (FRAGCOORD.xy / iResolution.xy) - .5;
	float t = TIME * .1 + ((.25 + .05 * sin(TIME * .1))/(length(uv.xy) + .07)) * 2.2;
	float si = sin(t);
	float co = cos(t);
	mat2 ma = mat2(vec2(co, si), vec2(-si, co));

	float v1, v2, v3;
	v1 = v2 = v3 = 0.0;
	
	float s = 0.0;
	for (int i = 0; i < 90; i++)
	{
		vec3 p = s * vec3(uv, 0.0);
		p.xy *= ma;
		p += vec3(.22, .3, s - 1.5 - sin(TIME * .13) * .1);
		for (int i = 0; i < 8; i++)	p = abs(p) / dot(p,p) - 0.659;
		v1 += dot(p,p) * .0015 * (1.8 + sin(length(uv.xy * 13.0) + .5  - TIME * .2));
		v2 += dot(p,p) * .0013 * (1.5 + sin(length(uv.xy * 14.5) + 1.2 - TIME * .3));
		v3 += length(p.xy*10.) * .0003;
		s  += .035;
	}
	
	float len = length(uv);
	v1 *= smoothstep(.7, .0, len);
	v2 *= smoothstep(.5, .0, len);
	v3 *= smoothstep(.9, .0, len);
	
	vec3 col = vec3( v3 * (1.5 + sin(TIME * .2) * .4),
					(v1 + v3) * .3,
					 v2) + smoothstep(0.2, .0, len) * .85 + smoothstep(.0, .6, v3) * .3;

	COLOR=vec4(min(pow(abs(col), vec3(1.2)), 1.0), 1.0);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_gj554"]
script = SubResource("CSharpScript_dxtts")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ijxml"]
shader = ExtResource("2_aj3u2")
shader_parameter/opacity = 1.0

[node name="Menu" type="Control"]
material = SubResource("ShaderMaterial_gj554")
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
offset_left = 1.0
offset_top = 1.0
offset_right = 1.0
offset_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_1lfjr")

[node name="TextureRect" type="TextureRect" parent="."]
material = SubResource("ShaderMaterial_ijxml")
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
texture = ExtResource("3_ctgpw")

[node name="VBoxContainer" type="VBoxContainer" parent="TextureRect"]
layout_mode = 1
anchors_preset = 2
anchor_top = 1.0
anchor_bottom = 1.0
offset_top = -101.0
offset_right = 124.0
grow_vertical = 0
